# Marca el codigo generado por Lombok con @lombok.Generated, para que JaCoCo
# (y SonarQube) excluyan automaticamente getters/setters/builders/equals/etc.
# de las metricas de cobertura. Solo cuenta el codigo escrito a mano.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
